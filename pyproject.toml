[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "nexo-invest"
version = "0.1.0"
description = "Aplicacao desktop educacional para investimentos simulados"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

